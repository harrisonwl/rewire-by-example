# Conversion into ReWire


> [!TIP]
	> The proper `rwc` incantation uses `--loadpath=...path-to-rwcrypto/src...`, as in:
> ```haskell
> rwc RW_AES256.hs --verilog --loadpath=/Users/bill/work/rwcrypto/src
> ```
